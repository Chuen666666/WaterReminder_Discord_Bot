{
    "isDM": true, //boolean indicating whether the bot is running in a DM (true) or a server (false)
    "IDS": [ID1, ID2, ...] //array of user IDs (for DM) or channel IDs (for server) to send reminders to
}